// Create styled components named UlStyled and LiStyled
import styled from "styled-components";

export const UlStyled = styled.ul`
display: grid;
grid-template-columns: repeat(2, minmax(200px, 1fr));
gap: 0px;
font-size: 1rem;
`
